@{ ViewData["Title"] = "FuelQuoteForm"; } 
@using Microsoft.AspNetCore.Authorization; 
@using Microsoft.AspNetCore.Mvc;
@using cosc_4353_project.Models;
@model cosc_4353_project.Models.FuelQuoteModel
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Fuel Quote Form</title>
  </head>
  <style>
    .form {
      background-color: #fff;
      padding: 20px 40px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 300px;
    }

    h2 {
      text-align: center;
      margin-bottom: 20px;
    }

    .input-group {
      margin-bottom: 15px;
    }

    label {
      margin-bottom: 5px;
    }
  </style>
  <body>
    <div class="form">
      <h2>Fuel Quote Form</h2>
        <form action="/FuelQuote/FuelQuoteFormDB" method="post">
        <label for="gallonsRequested">Gallons Requested:</label>
        <br>
            <input type="number" id="gallonsRequested" name="gallonsRequested" min="1" required>
        <br>
        <label for="deliveryAddress">Delivery Address:</label>
        <br>
            <input type="text" id="deliveryAddress" name="deliveryAddress" value="@Model.Client[0].Address1 @Model.Client[0].City @Model.Client[0].State @Model.Client[0].Zipcode" required>
        <br>
        <label for="deliveryDate">Delivery Date:</label>
        <br>
            <input type="date" id="deliveryDate" name="deliveryDate" required>
        <br>
        <label for="suggestedPrice">Suggested Price:</label>
        <br>
            <input type="number" id="suggestedPrice" name="suggestedPrice" required>
        <br>
        <label for="totalAmountDue">Total Amount Due:</label>
        <br>
            <input type="number" id="totalAmountDue" name="totalAmountDue" required>
        <br>
            <button type="submit" onclick="calculateTotalAmount()" style="background-color: #007BFF; padding: 8px 15px; border: none; border-radius: 4px; color: #fff; cursor: pointer;">Calculate</button>
      </form>
    </div>
    <script>
      //TODO: Add JavaScript function to perform the calculation
      function calculateTotalAmount() {
        // perform calculation here and set the value to the totalAmountDue input field
      }
    </script>
  </body>
</html>